apply from: '../modulebuild.gradle'

android {

    defaultConfig {

        if (isModule.toBoolean()) {

            applicationId "com.munch.module.main"
        }
    }
}

dependencies {
    implementation project(':libExpand')
    implementation project(':libApp')
}