<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="art"
            type="com.munch.project.one.dev.Articles" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        style="@style/AppTheme.Clickable"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/item_net_title"
            style="@style/AppTheme.Text.Title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/paddingDef"
            android:layout_marginTop="@dimen/paddingDef"
            android:layout_marginEnd="@dimen/paddingDef"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="@{art.title}"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.0"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="11111111111111111111111111111111111111111111111111" />

        <TextView
            android:id="@+id/item_net_author"
            style="@style/AppTheme.Text.Subtitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/paddingSmall"
            android:layout_marginEnd="@dimen/paddingDef"
            android:layout_marginBottom="@dimen/paddingDef"
            android:text="@{art.showName}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/item_net_title"
            tools:text="author" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/paddingDef"
            android:layout_marginTop="@dimen/paddingSmall"
            android:text="@{art.superChapterName}"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/item_net_title"
            tools:text="from" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>