<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/receive_log_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#45000000">

    <TextView
        android:id="@+id/receive_log_reduce"
        style="@style/AppTheme.Clickable"
        android:layout_width="35dp"
        android:layout_height="35dp"
        android:gravity="center"
        android:text="-"
        android:textColor="@color/black"
        android:textSize="22sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="HardcodedText" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/receive_log_rv"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/receive_log_reduce"
        tools:listitem="@layout/item_log_content" />

    <TextView
        android:id="@+id/receive_log_item_tv"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:gravity="center"
        android:visibility="invisible"
        app:layout_constraintBottom_toBottomOf="@id/receive_log_rv"
        app:layout_constraintEnd_toEndOf="@id/receive_log_rv"
        app:layout_constraintStart_toStartOf="@id/receive_log_rv"
        app:layout_constraintTop_toTopOf="@id/receive_log_rv" />

</androidx.constraintlayout.widget.ConstraintLayout>