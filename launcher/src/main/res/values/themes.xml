<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <!--如果无法引用，多重启-->
    <style name="BaseTheme" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/teal_200</item>
        <item name="colorPrimaryVariant">@color/teal_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <!--全局设置背景色-->
        <item name="android:windowBackground">@color/colorBg</item>

        <item name="android:windowAnimationStyle">@style/ActivityAnim</item>
    </style>

    <style name="ActivityAnim">
        <item name="android:activityOpenEnterAnimation">@anim/anim_open_in</item>
        <item name="android:activityOpenExitAnimation">@anim/anim_open_out</item>
        <item name="android:activityCloseEnterAnimation">@anim/anim_close_in</item>
        <item name="android:activityCloseExitAnimation">@anim/anim_close_out</item>
    </style>

    <!--主要的设置在此主题-->
    <style name="CommonTheme" parent="BaseTheme">

        <item name="pageBg">@color/colorBg</item>
    </style>

    <!--用于夜间模式，统一设置在CommonTheme，夜间模式差异则在夜间文件夹下的AppTheme-->
    <style name="AppTheme" parent="CommonTheme" />

    <style name="WallpaperTheme" parent="android:Theme.Wallpaper.NoTitleBar"/>
</resources>