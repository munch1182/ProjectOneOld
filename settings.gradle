pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()

        maven { url 'file://~/repository' }
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        maven { url 'file://~/repository' }
        println "${'file://${rootProject.rootDir}/repository'}"
    }
}
rootProject.name = "ProjectOne"
include ':p1'
include ':lib'
include ':libfast'
include ':launcher'
include ':wheelview'
include ':record'
