<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.munch.project.one.applib">

    <application
        android:name="com.munch.lib.fast.FastApp"
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_scheme"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.P1">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".recyclerview.RvSingleActivity" />
        <activity android:name=".recyclerview.RvMultiActivity" />
        <activity android:name=".TestWeightActivity" />
        <activity android:name=".weight.TestFlowLayoutActivity" />
        <activity android:name=".file.TestFileActivity" />
    </application>

</manifest>